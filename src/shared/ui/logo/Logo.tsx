import { Typography } from '@mui/material';

export const Logo = () => {
  return (
    <Typography
      variant="body1"
      color="secondary.main"
      sx={{
        marginRight: 'auto',
        fontSize: '1.5rem',
      }}
    >
      RCA
    </Typography>
  );
};
